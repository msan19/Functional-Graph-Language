using ASTLib;
using Hime.Redist;

namespace LexParserLib
{
    public class ASTBuilder
    {
        public AST GetAST(ASTNode root)
        {
            
            //return new AST();
        }
    }
}